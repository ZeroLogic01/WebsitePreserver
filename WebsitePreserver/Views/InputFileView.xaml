<UserControl x:Class="WebsitePreserver.Views.InputFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
       
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto" MaxWidth="250"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                    Style="{StaticResource TextBlockStyle}"
                    Text="{Binding InputFileVM.TbLabel, Mode=OneWay}"/>

            <TextBox  Text="Firefox profile:" IsReadOnly="True"
                     Grid.Column="1" BorderThickness="0"
                     VerticalAlignment="Center" HorizontalContentAlignment="Left"/>

            <TextBox Controls:TextBoxHelper.Watermark="Enter name of the Firefox profile (Required)" 
                     Grid.Column="2" BorderThickness="0" 
                     Text="{Binding 
                Path=InputFileVM.FirefoxProfileName,
            Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" HorizontalContentAlignment="Left"/>
        </Grid>

        <TextBox x:Name="tbExcelFile" Grid.Row="1" Grid.Column="0"
                         Style="{StaticResource TextBoxStyle}"
                         Text="{Binding 
            Path=InputFileVM.Path,
            Mode=TwoWay,UpdateSourceTrigger=PropertyChanged
            }" IsReadOnly="True"/>
        <Button Grid.Row="1" Grid.Column="1" 
                Content="{Binding Path=InputFileVM.BtnSelectLbl,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                Command="{Binding InputFileVM.SelectCommand}"/>
    </Grid>
</UserControl>
