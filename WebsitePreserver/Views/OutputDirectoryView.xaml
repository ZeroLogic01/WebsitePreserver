<UserControl x:Class="WebsitePreserver.Views.OutputDirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebsitePreserver.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <TextBlock  Grid.Row="0" Grid.Column="0"
                    Style="{StaticResource TextBlockStyle}"
                    Text="{Binding OutputDirectoryVM.TbLabel, Mode=OneWay}"/>
        <TextBox x:Name="tbOutputDir" Grid.Row="1" Grid.Column="0"
                         Style="{StaticResource TextBoxStyle}"
                         Text="{Binding Path=OutputDirectoryVM.Path,
            Mode=TwoWay,UpdateSourceTrigger=PropertyChanged
            }"/>
        <Button Grid.Row="1" Grid.Column="1"
                Content="{Binding Path=OutputDirectoryVM.BtnSelectLbl,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                Command="{Binding OutputDirectoryVM.SelectCommand}"/>

    </Grid>
</UserControl>
